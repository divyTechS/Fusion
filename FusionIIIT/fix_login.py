import re


if __name__ == "__main__":
    raise RuntimeError(
        "fix_login.py is deprecated. Apply login-related changes directly in the frontend "
        "Fusion-client project instead of rewriting files from this backend repository."
    )
